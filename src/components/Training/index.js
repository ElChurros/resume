import React from 'react';

import Section from '../Section';

export function Training(props) {
    return (
        <Section title={'Epitech'}>
            <span>Ecole de l'innovation</span>
        </Section>
    );
}

export default Training;